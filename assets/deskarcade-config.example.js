// Copy this file to deskarcade-config.js and fill in your Supabase project values.
// deskarcade-config.js is gitignored — never commit real keys to a public repo.
// The Supabase *anon* key is safe to expose in the browser when RLS is configured.

window.DESK_ARCADE_CONFIG = {
  supabaseUrl: "https://YOUR_PROJECT_REF.supabase.co",
  supabaseAnonKey: "YOUR_SUPABASE_ANON_KEY"
};
